The file to be modified is /etc/hostname, which is read at boot time to assign the system name.
Also, is recommended to modify the file /etc/hosts replacing the old hostname with the new one.
Finally, to apply the changes you have to reboot the computer or restart the hostname and the networking services.
